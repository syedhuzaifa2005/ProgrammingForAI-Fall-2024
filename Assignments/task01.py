import numpy as np

arr = np.arange(1, 11, 1)
print(f"Array:\n", arr)
sum = arr.sum()
print(f"Sum of Elements of the Array: {sum}")
mean = arr.mean()
print(f"The mean of Elements of the Array: {mean}")
product = arr.prod()
print(f"The product of Elements of the Array: {product}")